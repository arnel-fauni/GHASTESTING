name: GHAS Slack Notifications  # Add root name

on:
  # Your trigger events here (schedule, workflow_run, etc.)

jobs:
  notify-slack:
    runs-on: ubuntu-latest
    steps:
    - name: Fetch code scanning alerts  # Now properly nested
      # Your step content here
