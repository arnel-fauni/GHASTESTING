name: GHAS Slack Notifications
on: [push, pull_request]
permissions:
  contents: read
  security-events: read
  actions: read
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Slack Notification
        if: always()
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "🔍 *${{ github.workflow }}* completed!\n📊 *Status:* ${{ job.status }}\n📂 Repo: ${{ github.repository }}\n🔢 Run: #${{ github.run_number }}",
              "blocks": [
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*🛡️ Code Scanning:*\n✅ Check Security tab → Code scanning"
                    },
                    {
                      "type": "mrkdwn", 
                      "text": "*🔑 Secret Scanning:*\n✅ Automatic - View alerts"
                    }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "👉 <${{ github.server_url }}/${{ github.repository }}/security/code-scanning|View Code Scanning>\n👉 <${{ github.server_url }}/${{ github.repository }}/security/alerts|View Secret Alerts>"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "Security Tab" },
                      "url": "${{ github.server_url }}/${{ github.repository }}/security"
                    },
                    {
                      "type": "button", 
                      "text": { "type": "plain_text", "text": "View Run" },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
